# 隐私说明

最后复核：2026-07-30

Product Evidence Guard 的设计目标是在用户机器上分析商品资料。本文件明确区分
已经实现的数据处理方式与尚未完成的实际验证。

## 应用会读取什么

根据所选工作流，应用可能读取：

- TXT、Markdown、CSV、JSON、DOCX、XLSX 和带文字层的 PDF；
- 商品图片及可选的 `*.ocr.json` 旁路文件；
- 本地 OpenVINO 模型文件；
- 所选输出目录中已有的分析与确认状态。

输入商品文件以只读方式打开。当前分析代码不会重命名、编辑或删除这些文件。

## 应用会写入什么

应用将报告和状态写入用户选择的输出目录，默认位置为
`<输入目录>/.peg-output/`。这些工件可能包含敏感业务信息：

| 工件类型 | 可能包含的数据 |
|---|---|
| 候选与冲突报告 | 商品值、来源相对文件名、原文片段、定位信息、可信度估计和文件哈希 |
| HTML 报告 | 便于人工阅读的候选值和证据位置 |
| 分析状态 | 缓存候选、来源哈希及模型/设备签名 |
| 确认状态与审计 | candidate ID、来源文件与哈希、决定、理由和时间 |
| 已确认导出 | 仅包含用户明确确认且仍关联当前来源哈希的事实 |

报告为了可审计性会有意保留证据，它们不是匿名化导出。

应用不会自动删除这些文件。用户负责按照适用的业务制度保存或清理它们。
`.gitignore` 会排除常见输出、日志、模型和真实样本，但 `.gitignore` 不是访问
控制或数据防泄漏机制。

## 网络边界

已实现的分析路径使用本地解析器，并在配置时使用本地 OpenVINO 模型目录。当前
项目源码没有分析遥测客户端，也没有把商品文件上传到云端 OCR 或托管模型 API 的
实现路径。

下列分析以外的操作仍可能联网：

- 安装 Python 依赖；
- 从 Hugging Face 下载模型快照；
- 使用 Qoder 或其他宿主应用——其行为受各自服务与隐私条款约束。

`huggingface-hub` 是同时具备上传和下载能力的通用客户端库。Product Evidence
Guard 的模型获取流程只能使用下载操作，绝不能上传用户资料或生成报告。

依赖和模型在本地齐全后，已经在 `HF_HUB_OFFLINE=1`、
`TRANSFORMERS_OFFLINE=1`、`OPENVINO_TELEMETRY_DISABLED=1` 下完成真实本地
推理。最终工件 `<artifact-root>\test-real-model-release-20260730\` 状态为
`passed`，在 CPU 上生成 1 个保持 `pending` 的候选。这证明离线开关路径可用；
尚未执行防火墙阻断或抓包，因此不能声明零外连或 air-gapped 运行。

## Qoder 边界

Skill 可以指导 Qoder 运行本地命令，但 Qoder 是独立产品。安装本 Skill 不会改变
Qoder 自身的数据处理、账号或联网行为。不要为了触发 Skill 而把机密原文粘贴到
聊天中；应优先传递本地路径，并查看本地生成的报告。

目标环境已经通过 Qoder CLI 1.1.8 发现用户级 Skill 为 `Enabled`；账号仍未登录，
所以真实调用与数据流行为尚未验证。安装器的 allowlist、完整性、回滚和链接防护
合同已通过本地测试，只能证明技术安装边界，不证明 Qoder 会话中的实际数据流。

## 日志与控制台输出

当前命令行入口会在控制台输出结构化运行摘要或错误。报告按设计包含证据。任何
未来的后台服务或安装器日志都必须遵守：

- 不记录完整文档正文、模型提示词、原始模型响应、token 或秘密；
- 相对路径够用时，不记录完整客户绝对路径；
- 运维日志与证据报告分开；
- 限定保留期并使用受限的本地文件权限。

证明所有未来错误路径都满足这些规则的端到端审计仍待执行。

## 本地模型与缓存

模型文件不是用户文档，但它们是大型第三方工件并有独立许可证。模型目录和
Hugging Face 缓存位于证据输出之外。删除项目输出目录不一定会删除共享模型缓存。

## 用户责任

处理商品资料前：

1. 确认自己有权读取和分析这些资料；
2. 使用具备适当权限的本地账号与输出目录；
3. 避免把输入、输出、模型或日志目录意外同步到云服务；
4. 分享前检查报告，因为其中可能复现来源文字；
5. 保留期结束后删除输出和缓存；
6. 不要提交真实客户样本、报告或确认记录。

## 验证状态

| 隐私声明 | 状态 | 证据或缺口 |
|---|---|---|
| 核心解析与推理设计为本地处理 | **已完成** | 代码检查确认 |
| 项目代码没有分析遥测路径 | **已完成** | 代码检查确认 |
| 分析不会修改原文件 | **已完成** | 输入只读合同与代码检查 |
| 项目自身计划内网络行为只限依赖和模型下载 | **已完成** | 正式模型已下载；完整外连审计另列 |
| 本地 CPU 对真实公开标签图完成推理 | **已验证** | 最终离线工件状态 `passed`、候选 1 且保持 `pending`；不代表数据集准确率 |
| 冻结 Benchmark 不使用客户资料 | **已验证** | commit `06f8360` 使用 30 图、10 文档 synthetic 数据 |
| 三个离线/遥测环境变量下真实推理 | **已验证** | 本地依赖与模型齐全后成功 |
| 防火墙阻断时真实模型推理 | **未完成** | 尚未执行 |
| 抓包显示推理没有外连 | **未完成** | 尚未执行 |
| Qoder CLI 发现本 Skill | **已验证** | CLI 1.1.8，用户级，`Enabled` |
| Qoder 真实调用的数据流 | **待用户操作** | 账号未登录，且部分取决于 Qoder |
| 所有失败路径日志都不含来源片段 | **未完成** | 尚未完成端到端审计 |

请通过仓库的安全报告渠道报告隐私缺陷。公开 issue 中不要附上客户文档、含秘密的
模型输出或任何凭据。
