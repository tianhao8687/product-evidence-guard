# 标注与评分修订记录

初始 corpus SHA-256：`9fee51b52ef498e54fb252ad6bfe3ba1ab2947edd58537028635e942b972fe57`。
初始 development 基线完整保存于本地 `baseline-development-v2/report.json`。
该版初评分 57/80，不能直接当成修复前的最终正确包数。

2026-09-16，在查看 development 预测后、首次 holdout 执行前：

1. `01-06`：原答案把所有多档输出强制视为待确认。既有产品契约支持完整数值列表，
   实际输出 `[3.3, 5, 12] V` 未丢失任何档位。修正答案为已确认列表；不改引擎来制造待确认。
   输入字节不变，标题明确这是多档保留测试，不是缺少通道映射测试。
2. `07-08`：12 个月与 1 年为精确日历周期等价。既有契约规范化为 1 年，
   原答案遗漏了该契约，修正答案。不是改生产代码。
3. 评分器仅对开放字段值中的“数字与单位之间空格”做排版等价比较，例如
   `1310 nm` 与 `1310nm`；不猜字段、不换单位、不忽略条件/归属/状态。
4. 整数和同值浮点数已在首次基线前统一评分（1 与 1.0）；布尔值仍严格区分。

首次 holdout 结果 19/20，其中 `10-05` 唯一差异为华氏转换的浮点尾差：
`-8e-16` 对 `0`，`-40.00000000000001` 对 `-40`。未修改输入、gold 或生产算法；
评分器改为复用独立的单对单数值比较器（绝对容差 1e-9，相对容差 1e-12），
比旧 benchmark 的 1e-6 绝对容差更严格，可区分 1 微安差异。旧预测与新预测一起重评。
该 holdout 的预测现已被查看，后续重跑是回归，不再称为新盲测。

上述变化须同时重评旧预测与修复后的预测。原始初评分保留，禁止只展示修正后的新成绩。
这不是独立复标，所有 gold 均为助手标注；未做双人一致性验证。

既有回归中的 `n18` 原本将科学计数法 `1e3g` 当作不支持表达，预期待确认。
本轮实现有界科学计数法后，契约升级为已确认 `1000g`，保留输入并增加明确的
`contract_revision` 说明。首次旧回归 505 项中这一项失败，修订后纳入新回归；
没有删除该用例，也没有把旧运行结果改写成通过。

字节指纹说明：实际执行的 Windows fixture 为 CRLF，最终 SHA-256 为
`63308aa3be867915438d31f523f178f1eeecb3be137d502d5d145199d0b69ba9`；仓库按
`.gitattributes` 转成 LF 后为
`9f0bb886c6704b3c82dcfeb63e7ef4a1d888916bf616038fc3eb9cbe747915ec`。
两者解析后的 JSON 相同；不能把行尾差异误报为 gold 内容修改，也不改写历史执行哈希。
新 checkout 应重新运行，不能沿用本机旧检查点。机器证据同时记录两个指纹。
